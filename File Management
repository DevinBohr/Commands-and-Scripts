Get-ChildItem C:\ -Recurse	
    - Recursively lists every file and folder.

Get-ChildItem C:\ -Recurse | Sort Length -Descending | Select Name,Length -First 20	
    - Finds the largest files on a drive.

Remove-Item $env:TEMP\* -Recurse -Force	
    - Deletes the current user's temporary files.
